<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.back.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.back.pojo.User">
            <id property="userId" column="user_id" jdbcType="INTEGER"/>
            <result property="userName" column="user_name" jdbcType="VARCHAR"/>
            <result property="password" column="password" jdbcType="VARCHAR"/>
    </resultMap>
    <update id="updatePasswordByUserId">
        update users set password = #{newPassword} where user_id = #{id};
    </update>

    <select id="getAllUser" resultType="com.back.pojo.User">
        select * from users;
    </select>
    <select id="login" resultType="java.lang.Integer">

    </select>
    <select id="getUserByUsername" parameterType="string" resultType="com.back.pojo.User">
        select * from user where username = #{username}
    </select>

</mapper>
